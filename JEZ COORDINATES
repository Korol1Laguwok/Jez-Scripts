local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local localPlayer = Players.LocalPlayer
local camera = workspace.CurrentCamera

-- Функция защиты и привязки к CoreGui
local function protect(gui)
    if syn and syn.protect_gui then syn.protect_gui(gui) end
    gui.Parent = game:GetService("CoreGui")
end

-- === ЭКРАН ЗАГРУЗКИ (LOADING ANIMATION) ===
local LoadingGui = Instance.new("ScreenGui")
LoadingGui.Name = "JezLoading"
protect(LoadingGui)

local LoadFrame = Instance.new("Frame", LoadingGui)
LoadFrame.Size = UDim2.new(0, 260, 0, 100)
LoadFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
LoadFrame.AnchorPoint = Vector2.new(0.5, 0.5)
LoadFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
LoadFrame.BackgroundTransparency = 1
Instance.new("UICorner", LoadFrame).CornerRadius = UDim.new(0, 12)

local LoadStroke = Instance.new("UIStroke", LoadFrame)
LoadStroke.Color = Color3.fromRGB(0, 255, 150)
LoadStroke.Transparency = 1
LoadStroke.Thickness = 2

local TitleLabel = Instance.new("TextLabel", LoadFrame)
TitleLabel.Size = UDim2.new(1, 0, 0, 40)
TitleLabel.Position = UDim2.new(0, 0, 0, 15)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Text = "JEZ WAYPOINTS"
TitleLabel.TextColor3 = Color3.new(1, 1, 1)
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.TextSize = 18
TitleLabel.TextTransparency = 1

local CreditLabel = Instance.new("TextLabel", LoadFrame)
CreditLabel.Size = UDim2.new(1, 0, 0, 20)
CreditLabel.Position = UDim2.new(0, 0, 0, 42)
CreditLabel.BackgroundTransparency = 1
CreditLabel.Text = "by jezafes"
CreditLabel.TextColor3 = Color3.fromRGB(0, 255, 150)
CreditLabel.Font = Enum.Font.Gotham
CreditLabel.TextSize = 12
CreditLabel.TextTransparency = 1

local BarBg = Instance.new("Frame", LoadFrame)
BarBg.Size = UDim2.new(0.8, 0, 0, 6)
BarBg.Position = UDim2.new(0.1, 0, 0, 75)
BarBg.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
BarBg.BackgroundTransparency = 1
Instance.new("UICorner", BarBg).CornerRadius = UDim.new(1, 0)

local BarFill = Instance.new("Frame", BarBg)
BarFill.Size = UDim2.new(0, 0, 1, 0)
BarFill.BackgroundColor3 = Color3.fromRGB(0, 255, 150)
BarFill.BackgroundTransparency = 1
Instance.new("UICorner", BarFill).CornerRadius = UDim.new(1, 0)

-- Запуск анимации появления загрузчика
TweenService:Create(LoadFrame, TweenInfo.new(0.4, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {BackgroundTransparency = 0.2}):Play()
TweenService:Create(LoadStroke, TweenInfo.new(0.4), {Transparency = 0}):Play()
TweenService:Create(TitleLabel, TweenInfo.new(0.4), {TextTransparency = 0}):Play()
TweenService:Create(CreditLabel, TweenInfo.new(0.4), {TextTransparency = 0}):Play()
TweenService:Create(BarBg, TweenInfo.new(0.4), {BackgroundTransparency = 0.3}):Play()
TweenService:Create(BarFill, TweenInfo.new(0.4), {BackgroundTransparency = 0}):Play()

task.wait(0.3)

-- Плавное заполнение прогресс-бара
local fillTween = TweenService:Create(BarFill, TweenInfo.new(0.8, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {Size = UDim2.new(1, 0, 1, 0)})
fillTween:Play()
fillTween.Completed:Wait()

task.wait(0.2)

-- Анимация исчезновения загрузчика
local fadeOutInfo = TweenInfo.new(0.4, Enum.EasingStyle.Quart, Enum.EasingDirection.In)
TweenService:Create(LoadFrame, fadeOutInfo, {BackgroundTransparency = 1, Size = UDim2.new(0, 220, 0, 80)}):Play()
TweenService:Create(LoadStroke, fadeOutInfo, {Transparency = 1}):Play()
TweenService:Create(TitleLabel, fadeOutInfo, {TextTransparency = 1}):Play()
TweenService:Create(CreditLabel, fadeOutInfo, {TextTransparency = 1}):Play()
TweenService:Create(BarBg, fadeOutInfo, {BackgroundTransparency = 1}):Play()
TweenService:Create(BarFill, fadeOutInfo, {BackgroundTransparency = 1}):Play()

task.wait(0.4)
LoadingGui:Destroy()

-- === УЛЬТРА-БЫСТРАЯ ФУНКЦИЯ ПУТИ ===
local PathfindingService = game:GetService("PathfindingService")

local function createVisualWaypoint(position, name)
    local folder = workspace:FindFirstChild("JezWaypoints") or Instance.new("Folder", workspace)
    folder.Name = "JezWaypoints"
    
    local part = Instance.new("Part")
    part.Name = name .. "_Target"
    part.Position = position + Vector3.new(0, 0.5, 0)
    part.Size = Vector3.new(2, 2, 2)
    part.Shape = Enum.PartType.Ball
    part.Material = Enum.Material.Neon
    part.Color = Color3.fromRGB(0, 255, 100)
    part.Anchored = true
    part.CanCollide = false
    part.Parent = folder
    
    local billboard = Instance.new("BillboardGui", part)
    billboard.Size = UDim2.new(0, 100, 0, 30)
    billboard.AlwaysOnTop = true
    billboard.StudsOffset = Vector3.new(0, 3, 0)
    local label = Instance.new("TextLabel", billboard)
    label.Size = UDim2.new(1, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.Text = name
    label.TextColor3 = Color3.new(1, 1, 1)
    label.Font = Enum.Font.GothamBold
    label.TextSize = 14

    local routeFolder = Instance.new("Folder", part)
    routeFolder.Name = "Route"

    -- Настройки быстрого пути
    local path = PathfindingService:CreatePath({
        AgentRadius = 2,
        AgentHeight = 5,
        AgentCanJump = true,
        WaypointSpacing = 8,
    })

    local lastPos = Vector3.new(0,0,0)

    local function updatePath()
        if not part or not part.Parent then return end
        
        local character = localPlayer.Character
        local hrp = character and character:FindFirstChild("HumanoidRootPart")
        if not hrp then return end

        if (hrp.Position - lastPos).Magnitude < 10 then return end
        lastPos = hrp.Position

        local success, _ = pcall(function()
            path:ComputeAsync(hrp.Position, position)
        end)

        if success and path.Status == Enum.PathStatus.Success then
            routeFolder:ClearAllChildren()
            local waypoints = path:GetWaypoints()
            
            local maxWaypoints = math.min(#waypoints, 15) 
            
            for i = 1, maxWaypoints - 1 do
                local p1 = waypoints[i].Position
                local p2 = waypoints[i+1].Position
                
                local line = Instance.new("Part")
                line.Size = Vector3.new(0.2, 0.2, (p1 - p2).Magnitude)
                line.CFrame = CFrame.new(p1:Lerp(p2, 0.5), p2)
                line.Anchored = true
                line.CanCollide = false
                line.Material = Enum.Material.Neon
                line.Color = Color3.fromRGB(0, 255, 150)
                line.Transparency = 0.7
                line.Parent = routeFolder
            end
        end
    end

    task.spawn(function()
        while part and part.Parent do
            updatePath()
            task.wait(0.5)
        end
    end)
    
    return part
end

-- Создаем контейнеры
local CoordsGui = Instance.new("ScreenGui")
CoordsGui.Name = "JezMain"
protect(CoordsGui)

local TPGui = Instance.new("ScreenGui")
TPGui.Name = "JezTP"
protect(TPGui)

-- === 1. ГЛАВНАЯ ПАНЕЛЬ КООРДИНАТ (HUD) ===
local MainFrame = Instance.new("Frame", CoordsGui)
MainFrame.Size = UDim2.new(0, 210, 0, 85) 
MainFrame.Position = UDim2.new(0.98, -210, 0.98, -85) 
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
MainFrame.BackgroundTransparency = 0.2
MainFrame.Active = true
MainFrame.Draggable = true 
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 10)

-- ГЛАВНЫЙ КРЕСТИК (ЗАКРЫВАЕТ ВСЁ)
local MasterClose = Instance.new("TextButton", MainFrame)
MasterClose.Size = UDim2.new(0, 35, 0, 35)
MasterClose.Position = UDim2.new(1, -40, 0, 5)
MasterClose.BackgroundColor3 = Color3.fromRGB(255, 60, 60)
MasterClose.Text = "×"
MasterClose.TextColor3 = Color3.new(1, 1, 1)
MasterClose.TextSize = 30
MasterClose.Font = Enum.Font.GothamBold
Instance.new("UICorner", MasterClose).CornerRadius = UDim.new(0, 8)

MasterClose.MouseButton1Click:Connect(function()
    local folder = workspace:FindFirstChild("JezWaypoints")
    if folder then
        folder:Destroy()
    end
    CoordsGui:Destroy()
    TPGui:Destroy()
end)

local CoordsLabel = Instance.new("TextLabel", MainFrame)
CoordsLabel.Size = UDim2.new(1, -75, 0, 40)
CoordsLabel.Position = UDim2.new(0, 5, 0, 0)
CoordsLabel.BackgroundTransparency = 1
CoordsLabel.Text = "Loading..."
CoordsLabel.TextColor3 = Color3.new(1, 1, 1)
CoordsLabel.Font = Enum.Font.GothamBold
CoordsLabel.TextSize = 14
CoordsLabel.TextXAlignment = Enum.TextXAlignment.Center

local CopyBtn = Instance.new("TextButton", MainFrame)
CopyBtn.Size = UDim2.new(0, 30, 0, 30)
CopyBtn.Position = UDim2.new(1, -75, 0, 5)
CopyBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
CopyBtn.Text = "C"
CopyBtn.TextColor3 = Color3.new(1, 1, 1)
CopyBtn.Font = Enum.Font.SourceSansBold
CopyBtn.TextSize = 18
Instance.new("UICorner", CopyBtn)

local PickBtn = Instance.new("TextButton", MainFrame)
PickBtn.Size = UDim2.new(0, 190, 0, 30)
PickBtn.Position = UDim2.new(0, 10, 0, 45)
PickBtn.Text = "ВЫБРАТЬ ТОЧКУ (КЛИК)"
PickBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
PickBtn.TextColor3 = Color3.new(1, 1, 1)
PickBtn.Font = Enum.Font.GothamBold
PickBtn.TextSize = 11
Instance.new("UICorner", PickBtn)

-- Логика HUD
local isPicking = false
local function getPointedCoordinates()
    local mousePos = UserInputService:GetMouseLocation()
    local unitRay = camera:ViewportPointToRay(mousePos.X, mousePos.Y)
    local result = workspace:Raycast(unitRay.Origin, unitRay.Direction * 5000)
    if result then
        local pos = result.Position
        local formatted = string.format("%.1f, %.1f, %.1f", pos.X, pos.Y, pos.Z)
        if setclipboard then setclipboard(formatted) end
        return formatted
    end
    return nil
end

PickBtn.MouseButton1Click:Connect(function()
    isPicking = true
    PickBtn.Text = "КЛИКНИ НА КАРТУ..."
    PickBtn.BackgroundColor3 = Color3.fromRGB(200, 150, 0)
end)

UserInputService.InputBegan:Connect(function(input, processed)
    if isPicking and not processed then
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            local coords = getPointedCoordinates()
            if coords then
                isPicking = false
                PickBtn.Text = "СКОПИРОВАНО!"
                PickBtn.BackgroundColor3 = Color3.fromRGB(0, 150, 80)
                task.delay(2, function()
                    PickBtn.Text = "ВЫБРАТЬ ТОЧКУ (КЛИК)"
                    PickBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
                end)
            end
        end
    end
end)

CopyBtn.MouseButton1Click:Connect(function()
    if localPlayer.Character and localPlayer.Character:FindFirstChild("HumanoidRootPart") then
        local pos = localPlayer.Character.HumanoidRootPart.Position
        local formatPos = string.format("%.1f, %.1f, %.1f", pos.X, pos.Y, pos.Z)
        if setclipboard then setclipboard(formatPos) end
        CopyBtn.Text = "ok"; CopyBtn.BackgroundColor3 = Color3.fromRGB(0, 255, 150)
        task.delay(0.7, function() CopyBtn.Text = "C"; CopyBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 40) end)
    end
end)

RunService.RenderStepped:Connect(function()
    if not CoordsGui.Parent then return end
    if localPlayer.Character and localPlayer.Character:FindFirstChild("HumanoidRootPart") then
        local pos = localPlayer.Character.HumanoidRootPart.Position
        CoordsLabel.Text = string.format("%.1f | %.1f | %.1f", pos.X, pos.Y, pos.Z)
    end
end)

-- === 2. ОКНО ТЕЛЕПОРТА ===
local TPFrame = Instance.new("Frame", TPGui)
TPFrame.Size = UDim2.new(0, 240, 0, 300)
TPFrame.Position = UDim2.new(0.98, -240, 0.5, -150)
TPFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
TPFrame.BackgroundTransparency = 0.2
TPFrame.Draggable = true; TPFrame.Active = true
Instance.new("UICorner", TPFrame)

-- Вотемарка by jezafes внизу окна телепорта
local FooterLabel = Instance.new("TextLabel", TPFrame)
FooterLabel.Size = UDim2.new(1, -20, 0, 15)
FooterLabel.Position = UDim2.new(0, 10, 1, -18)
FooterLabel.BackgroundTransparency = 1
FooterLabel.Text = "by jezafes"
FooterLabel.TextColor3 = Color3.fromRGB(120, 120, 120)
FooterLabel.Font = Enum.Font.Gotham
FooterLabel.TextSize = 10
FooterLabel.TextXAlignment = Enum.TextXAlignment.Center

local TPInput = Instance.new("TextBox", TPFrame)
TPInput.Size = UDim2.new(0.7, -10, 0, 40); TPInput.Position = UDim2.new(0, 10, 0, 15)
TPInput.PlaceholderText = "Координаты..."; TPInput.Text = ""; TPInput.TextSize = 16
TPInput.Font = Enum.Font.SourceSansBold; TPInput.BackgroundColor3 = Color3.fromRGB(30, 30, 30); TPInput.TextColor3 = Color3.new(1,1,1)
Instance.new("UICorner", TPInput)

local TPBtn = Instance.new("TextButton", TPFrame)
TPBtn.Size = UDim2.new(0.3, -15, 0, 40); TPBtn.Position = UDim2.new(0.7, 5, 0, 15)
TPBtn.Text = "TP"; TPBtn.TextSize = 18; TPBtn.Font = Enum.Font.SourceSansBold
TPBtn.BackgroundColor3 = Color3.fromRGB(0, 255, 150)
Instance.new("UICorner", TPBtn)

local TimeInput = Instance.new("TextBox", TPFrame)
TimeInput.Size = UDim2.new(0.3, -10, 0, 40); TimeInput.Position = UDim2.new(0, 10, 0, 65)
TimeInput.Text = "1"; TimeInput.PlaceholderText = "Сек"; TimeInput.TextSize = 18
TimeInput.Font = Enum.Font.SourceSansBold; TimeInput.BackgroundColor3 = Color3.fromRGB(30, 30, 30); TimeInput.TextColor3 = Color3.new(1,1,1)
Instance.new("UICorner", TimeInput)

local AutoBtn = Instance.new("TextButton", TPFrame)
AutoBtn.Size = UDim2.new(0.7, -15, 0, 40); AutoBtn.Position = UDim2.new(0.3, 5, 0, 65)
AutoBtn.Text = "AUTO: OFF"; AutoBtn.TextSize = 18; AutoBtn.Font = Enum.Font.SourceSansBold
AutoBtn.BackgroundColor3 = Color3.fromRGB(100, 100, 100); AutoBtn.TextColor3 = Color3.new(1,1,1)
Instance.new("UICorner", AutoBtn)

local SaveBtn = Instance.new("TextButton", TPFrame)
SaveBtn.Size = UDim2.new(0.5, -12, 0, 30)
SaveBtn.Position = UDim2.new(0, 10, 0, 110)
SaveBtn.Text = "МОЯ ТОЧКА"
SaveBtn.BackgroundColor3 = Color3.fromRGB(70, 70, 200)
SaveBtn.TextColor3 = Color3.new(1, 1, 1)
SaveBtn.Font = Enum.Font.GothamBold
SaveBtn.TextSize = 12 
Instance.new("UICorner", SaveBtn)

local CreateAtCoordsBtn = Instance.new("TextButton", TPFrame)
CreateAtCoordsBtn.Size = UDim2.new(0.5, -12, 0, 30)
CreateAtCoordsBtn.Position = UDim2.new(0.5, 2, 0, 110)
CreateAtCoordsBtn.Text = "МЕТКА ПО КООРД."
CreateAtCoordsBtn.BackgroundColor3 = Color3.fromRGB(0, 120, 0)
CreateAtCoordsBtn.TextColor3 = Color3.new(1, 1, 1)
CreateAtCoordsBtn.Font = Enum.Font.GothamBold
CreateAtCoordsBtn.TextSize = 10
Instance.new("UICorner", CreateAtCoordsBtn)

-- Создаем список для меток (уменьшили высоту на 15 пикселей под футер)
local scrollFrame = Instance.new("ScrollingFrame", TPFrame)
scrollFrame.Position = UDim2.new(0, 10, 0, 145)
scrollFrame.Size = UDim2.new(1, -20, 0, 130) 
scrollFrame.BackgroundTransparency = 0.5
scrollFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
scrollFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
scrollFrame.ScrollBarThickness = 3
Instance.new("UIListLayout", scrollFrame).Padding = UDim.new(0, 5)
Instance.new("UICorner", scrollFrame)

local isAuto = false
local function doTeleport()
    local coords = {}
    for num in string.gmatch(TPInput.Text:gsub(",", " "), "[%d%.%-]+") do 
        table.insert(coords, tonumber(num)) 
    end
    if #coords >= 3 and localPlayer.Character and localPlayer.Character:FindFirstChild("HumanoidRootPart") then
        localPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(Vector3.new(coords[1], coords[2], coords[3]) + Vector3.new(0, 3, 0))
        return true
    end
    return false
end

TPBtn.MouseButton1Click:Connect(function() 
    if doTeleport() then 
        TPBtn.Text = "OK!" 
        task.delay(1, function() TPBtn.Text = "TP" end) 
    end 
end)

AutoBtn.MouseButton1Click:Connect(function()
    isAuto = not isAuto
    AutoBtn.Text = isAuto and "AUTO: ON" or "AUTO: OFF"
    AutoBtn.BackgroundColor3 = isAuto and Color3.fromRGB(255, 80, 80) or Color3.fromRGB(100, 100, 100)
    
    if isAuto then
        task.spawn(function()
            while isAuto and TPGui.Parent do 
                doTeleport()
                task.wait(tonumber(TimeInput.Text) or 1) 
            end
        end)
    end
end)

SaveBtn.MouseButton1Click:Connect(function()
    if localPlayer.Character and localPlayer.Character:FindFirstChild("HumanoidRootPart") then
        local pos = localPlayer.Character.HumanoidRootPart.Position
        local name = "Метка " .. (#scrollFrame:GetChildren() - 1)
        
        local visualMarker = createVisualWaypoint(pos, name)
        
        local MarkContainer = Instance.new("Frame", scrollFrame)
        MarkContainer.Size = UDim2.new(1, -5, 0, 30)
        MarkContainer.BackgroundTransparency = 1
        MarkContainer.Name = name .. "_Container"
        
        local NameInput = Instance.new("TextBox", MarkContainer)
        NameInput.Size = UDim2.new(1, -75, 1, 0)
        NameInput.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
        NameInput.Text = name
        NameInput.TextColor3 = Color3.new(1, 1, 1)
        NameInput.Font = Enum.Font.GothamBold
        NameInput.TextSize = 10
        NameInput.ClearTextOnFocus = false
        Instance.new("UICorner", NameInput)
        
        local TpBtn = Instance.new("TextButton", MarkContainer)
        TpBtn.Size = UDim2.new(0, 35, 1, 0)
        TpBtn.Position = UDim2.new(1, -70, 0, 0)
        TpBtn.BackgroundColor3 = Color3.fromRGB(0, 100, 200)
        TpBtn.Text = "TP"
        TpBtn.TextColor3 = Color3.new(1, 1, 1)
        TpBtn.Font = Enum.Font.GothamBold
        Instance.new("UICorner", TpBtn)

        local DelBtn = Instance.new("TextButton", MarkContainer)
        DelBtn.Size = UDim2.new(0, 30, 1, 0)
        DelBtn.Position = UDim2.new(1, -30, 0, 0)
        DelBtn.BackgroundColor3 = Color3.fromRGB(150, 0, 0)
        DelBtn.Text = "X"
        DelBtn.TextColor3 = Color3.new(1, 1, 1)
        DelBtn.Font = Enum.Font.GothamBold
        Instance.new("UICorner", DelBtn)

        NameInput.FocusLost:Connect(function(enterPressed)
            local newName = NameInput.Text
            if newName ~= "" then
                MarkContainer.Name = newName .. "_Container"
                if visualMarker and visualMarker:FindFirstChild("BillboardGui") then
                    visualMarker.BillboardGui.TextLabel.Text = newName
                end
            end
        end)

        TpBtn.MouseButton1Click:Connect(function()
            if localPlayer.Character and localPlayer.Character:FindFirstChild("HumanoidRootPart") then
                localPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(pos + Vector3.new(0, 3, 0))
            end
        end)

        DelBtn.MouseButton1Click:Connect(function()
            if visualMarker then visualMarker:Destroy() end
            MarkContainer:Destroy()
            task.wait(0.1)
            scrollFrame.CanvasSize = UDim2.new(0, 0, 0, #scrollFrame:GetChildren() * 35)
        end)
        
        scrollFrame.CanvasSize = UDim2.new(0, 0, 0, #scrollFrame:GetChildren() * 35)
        
        SaveBtn.Text = "МЕТКА СОЗДАНА!"; SaveBtn.BackgroundColor3 = Color3.fromRGB(0, 200, 100)
        task.delay(1.5, function() 
            SaveBtn.Text = "МОЯ ТОЧКА"
            SaveBtn.BackgroundColor3 = Color3.fromRGB(70, 70, 200) 
        end)
    end
end)

CreateAtCoordsBtn.MouseButton1Click:Connect(function()
    local coords = {}
    for num in string.gmatch(TPInput.Text:gsub(",", " "), "[%d%.%-]+") do
        table.insert(coords, tonumber(num))
    end

    if #coords >= 3 then
        local targetPos = Vector3.new(coords[1], coords[2], coords[3])
        local name = "Метка (Коорд) " .. (#scrollFrame:GetChildren() - 1)
        
        local visualMarker = createVisualWaypoint(targetPos, name)
        
        local MarkContainer = Instance.new("Frame", scrollFrame)
        MarkContainer.Size = UDim2.new(1, -5, 0, 30)
        MarkContainer.BackgroundTransparency = 1
        MarkContainer.Name = name .. "_Container"

        local Mark = Instance.new("TextButton", MarkContainer)
        Mark.Size = UDim2.new(1, -40, 1, 0)
        Mark.BackgroundColor3 = Color3.fromRGB(40, 60, 40)
        Mark.Text = name .. " | TP"
        Mark.TextColor3 = Color3.new(1, 1, 1)
        Mark.Font = Enum.Font.GothamBold
        Mark.TextSize = 10
        Mark.ZIndex = 2 
        Instance.new("UICorner", Mark)

        local DelBtn = Instance.new("TextButton", MarkContainer)
        DelBtn.Size = UDim2.new(0, 30, 1, 0)
        DelBtn.Position = UDim2.new(1, -30, 0, 0) 
        DelBtn.BackgroundColor3 = Color3.fromRGB(180, 0, 0)
        DelBtn.Text = "X"
        DelBtn.TextColor3 = Color3.new(1, 1, 1)
        DelBtn.Font = Enum.Font.GothamBold
        DelBtn.ZIndex = 3 
        Instance.new("UICorner", DelBtn)

        Mark.MouseButton1Click:Connect(function()
            if localPlayer.Character and localPlayer.Character:FindFirstChild("HumanoidRootPart") then
                localPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(targetPos + Vector3.new(0, 3, 0))
            end
        end)

        DelBtn.MouseButton1Click:Connect(function()
            if visualMarker then visualMarker:Destroy() end
            MarkContainer:Destroy()
            task.wait(0.1)
            scrollFrame.CanvasSize = UDim2.new(0, 0, 0, #scrollFrame:GetChildren() * 35)
        end)

        scrollFrame.CanvasSize = UDim2.new(0, 0, 0, #scrollFrame:GetChildren() * 35)
        
        CreateAtCoordsBtn.Text = "ГОТОВО!"; CreateAtCoordsBtn.BackgroundColor3 = Color3.fromRGB(0, 200, 100)
        task.delay(1, function() 
            CreateAtCoordsBtn.Text = "МЕТКА ПО КООРД."
            CreateAtCoordsBtn.BackgroundColor3 = Color3.fromRGB(0, 120, 0) 
        end)
    end
end)
